# 项目说明书 · 风电功率预测

> 本文档讲清项目每一步的**原理与动机**，建议配合 `src/` 下的代码一起读。
>
> 建议阅读顺序：先跑一遍 `python run.py` 看结果，再对照本文理解每个环节。

---

## 目录

1. [这个项目在做什么](#一这个项目在做什么)
2. [为什么选这个方向](#二为什么选这个方向)
3. [数据集：GEFCom2014 是什么](#三数据集gefcom2014-是什么)
4. [数据长什么样](#四数据长什么样)
5. [项目的文件结构](#五项目的文件结构)
6. [核心概念（重点章节）](#六核心概念重点章节)
7. [怎么运行](#七怎么运行)
8. [结果怎么读](#八结果怎么读)
9. [面试时怎么讲](#九面试时怎么讲)
10. [接下来可以做什么](#十接下来可以做什么)

---

## 一、这个项目在做什么

**一句话：用数值天气预报（NWP）的风场预报，预测风电场的发电出力。**

输入：未来时刻的风速、风向（预报值）
输出：未来时刻的发电出力（0~1 归一化）

**它解决的是真问题**：电网调度必须提前知道明天风电能发多少电。
预测准了，电网可以少留备用火电，省下的是**真金白银**（后面细说）。

---

## 二、为什么选这个方向

三个理由，你都该能背下来：

**1. 它是你专业能直接变现的地方**
风电出力的物理原因**就是风**。你学的大气边界层、风切变、湍流，
在这里不是背景知识，是**核心竞争力**。

**2. 它是刚需，有硬指标**
国家对风电场功率预测有**考核标准**，预测准确率不达标要罚款。
所以这个岗位是"必须有"，不是"锦上添花"。

**3. 它的技术门槛和你的条件匹配**
不需要博士，本科能入行；但需要**懂物理 + 会数据**，
这两样你都有基础。而纯 CS 出身的人缺物理，纯气象出身的人缺工程 ——
**交叉点上人少，这就是你的位置。**

---

## 三、数据集：GEFCom2014 是什么

**GEFCom 全称 Global Energy Forecasting Competition（全球能源预测竞赛）**，
是能源预测领域最权威的国际竞赛，IEEE 主办。

**2014 年那一届有四条赛道**：电力负荷、**风电**、光伏、电价。

**为什么用竞赛数据而不是随便找个数据集？**

| 理由 | 说明 |
|---|---|
| **公认** | 论文里都在用，你说"我用了 GEFCom2014"，同行立刻知道是什么档次 |
| **真实** | 真实风电场数据 + 真实 NWP 预报，不是模拟生成的 |
| **有基准** | 别人有结果可比，你能说"我做到了什么水平" |
| **有难度** | 数据里有缺测、有异常，不是洗干净的教学数据 |

**数据来源**：GitHub 镜像仓库 `greenlytics/gefcom2014-wind`
（`data/download.py` 会自动下载）

---

## 四、数据长什么样

每个风场一个 CSV，长这样：

```
ZONEID,TIMESTAMP,TARGETVAR,U10,V10,U100,V100
1,20120101 1:00,0,2.12460013902668,-2.68196636902142,2.86427959225713,-3.66607576475047
1,20120101 2:00,0.0548791196734229,2.52169465408196,-1.79696009092255,3.34485867183801,-2.46476146094415
```

| 列 | 含义 | 你要知道的 |
|---|---|---|
| `ZONEID` | 风电场编号 | 一共 10 个风场 |
| `TIMESTAMP` | 时间 | **格式有坑**，见下 |
| `TARGETVAR` | **目标变量** | 归一化出力 0~1（不是 MW！） |
| `U10` `V10` | 10 米高度的风**矢量分量** | 东向 / 北向 |
| `U100` `V100` | 100 米高度的风矢量分量 | 接近风机轮毂高度 |

### ⚠️ 三个坑（这三个就是你 README「踩过的坑」那一节）

**坑 1：时间戳格式不规范**
`20120101 1:00` —— 小时**没补零**。直接用 `pd.to_datetime` 会解析失败，
或者把 `1:00` 当成时区。必须自己拆。
→ 见 `src/loader.py` 的 `_parse_timestamp`

**坑 2：给的是 U/V 分量，不是风速**
这是最容易犯的错。风速是矢量的模：

```
ws = √(U² + V²)
```

**不能**只看 U、不能取绝对值、不能当成风速直接用。
同一组 U 配不同 V，实际风速差很多。
→ 见 `src/features.py` 的 `wind_speed`

**坑 3：归一化出力不能直接换成电量**
`TARGETVAR` 是 0~1，要知道装机容量才能算 MW。
所以评估指标要按**归一化口径**算，别乱乘。

---

## 五、项目的文件结构

```
风电功率预测/
├── README.md              ← 给面试官看（放 GitHub 首页）
├── 说明书.md               ← 给你自己看（就是本文件）
├── requirements.txt
├── run.py                 ← 一键运行入口
├── data/
│   ├── download.py        ← 数据下载（带镜像兜底）
│   └── raw/               ← 数据放这里（不进 git）
├── src/
│   ├── loader.py          ← 读数据、解时间戳
│   ├── features.py        ← ★特征工程（物理量还原）
│   ├── baselines.py       ← ★物理基线（项目灵魂）
│   ├── models.py          ← 机器学习模型
│   ├── evaluate.py        ← 评估（国标口径）
│   └── plots.py           ← 出图
└── results/               ← 输出：对比表 + 图
```

**★ 标的是重点**：`features.py` 和 `baselines.py` 是你区别于其他候选人的地方。

---

## 六、核心概念（重点章节）

这一节是**面试会问的东西**，逐条读懂。

---

### 6.1 为什么出力与风速是「三次方」关系

风里携带的功率：

```
P = ½ · ρ · A · v³ · Cp
```

- `ρ` 空气密度
- `A` 叶片扫风面积
- `v` 风速
- `Cp` 风能利用系数（贝兹极限 0.593，实际约 0.4~0.5）

**关键在 `v³`**：风速翻倍，可用功率变成 **8 倍**。

**这意味着什么**：风速预测的误差会被**放大三倍**。所以风速预测精度
直接决定功率预测精度 —— 这就是为什么气象精度对风电这么要命。

> 📌 **面试话术**：「出力与风速是三次方关系，所以风速的误差会被放大，
> 这就是为什么气象预报精度是风电预测的天花板。」

---

### 6.2 什么是「功率曲线」

把「风速 → 出力」的关系画出来，就是功率曲线，它长这样：

```
出力
1.0 ┤                    ┌────────────┐
    │                   ╱              ╲
    │                 ╱                  ╲
    │               ╱                      ╲
0.0 ┤────────────╱                          ╲────────
    └────┬───────┬──────────┬────────┬──────┬──────
        0      切入        额定     切出
              3m/s      12~14m/s   25m/s
```

**四段**：
| 段 | 风速 | 物理原因 |
|---|---|---|
| 停机 | < 切入风速 | 风太小，推不动叶片 |
| 爬坡段 | 切入~额定 | 出力 ∝ v³，**这一段最活跃** |
| 额定 | 额定~切出 | 变桨控制，**出力锁定在额定值** |
| 停机 | > 切出风速 | 风太大，**保护性停机**（怕叶片损坏） |

**为什么这个重要**：
- 新人做项目常常直接线性回归 → **抓不到切入和切出** → 低风速段高估、切出时严重虚高
- 正确做法是**按风速分箱取中位数**拟合经验功率曲线（风电行业标准做法）
- 这就是 `src/baselines.py::PowerCurveBaseline` 干的事

---

### 6.3 什么是「风切变指数 α」，为什么它是你的杀手锏

风速随高度变化，遵循**幂律**：

```
ws(z) = ws(z_ref) · (z / z_ref)^α
```

反解出 α：

```
α = ln(ws100 / ws10) / ln(100 / 10)
```

**物理含义**（大气边界层的东西，你学过）：

| α 值 | 对应下垫面 / 层结 |
|---|---|
| ≈ 0.14 | 开阔海面，中性层结（1/7 律） |
| ≈ 0.25 | 草原、农田 |
| ≈ 0.3+ | 城市、森林，或**夜间稳定边界层** |

**为什么加这个特征厉害**：

1. **风机轮毂在 100m，数据只给 10m 和 100m 两个高度** —— 中间层的
   信息全靠 α 捕捉
2. **α 随大气稳定度变化**：白天太阳加热、混合层发展、α 小；夜间
   地面辐射冷却、逆温、α 大。**这是真实的物理信号，模型能学到**
3. **纯 CS 背景的候选人不会想到加这个特征** —— 这就是你的差异化

> 📌 **面试话术**：「我加了一个风切变指数作为特征，因为风机在 100 米，
> 而大气边界层的风速廓线随稳定度变化，白天和夜间的 α 不一样。
> 这个特征让模型能区分不同层结条件。」

---

### 6.4 为什么必须「按时间切分」，不能随机切分

**这是本项目最容易出错、也最容易被面试官抓的地方。**

假设你做随机切分（`train_test_split`）：

- 时间序列是**自相关**的：今天和明天高度相关
- 随机切分会让**明天的数据进入训练集**，去预测今天
- 结果：指标虚高到 RMSE ≈ 0.02，**看起来完美，实际一文不值**
- 因为你上线时**永远拿不到未来的数据**

正确做法：**切在某个时间点上，用过去预测未来。**

```python
cut = timestamp.quantile(1 - test_ratio)
train = data[timestamp <= cut]     # 过去
test  = data[timestamp >  cut]     # 未来
```

→ 见 `run.py::split_by_time`

> 📌 **面试话术**：「我特意做了时间序列切分而不是随机切分，
> 因为随机切分会导致未来信息泄漏，指标会虚高但上线就废。
> 这是时间序列预测最常见的坑。」

**这句话说出来，面试官会知道你做过真东西。**

---

### 6.5 为什么要有「持续性基线」

持续性预测（persistence）就是：

> **预测 t+1 时刻的出力 = t 时刻的实际出力**

它是最笨的预测，但**所有时间序列预测都必须跟它比**。

**为什么**：风是连续的，一小时内的变化通常不大。
所以持续性在 1 小时预测上其实**很强**。很多论文号称模型提升 30%，
其实根本没跟持续性比 —— 一比就露馅。

**你的项目里两条思路都要有**：
- 持续性 = 最低门槛（打不过它说明模型白做了）
- 物理基线 = 行业标准
- ML 模型 = 你的贡献

**三层对比，才叫完整。**

---

### 6.6 评估指标为什么用「准确率」而不是 R²

**国标口径**（新能源场站并网考核在用）：

```
准确率 = (1 - RMSE / 装机容量) × 100%
合格率 = 单点误差 ≤ 25%×装机容量 的样本占比
```

因为数据是归一化的，装机容量取 1，所以：

```
准确率 = (1 - RMSE) × 100%
```

**为什么不看 R²**：风电出力有大量 0 值（无风时），R² 会显得很好看，
但它**不反映调度关心的东西**。调度关心的是"你预测的数能信几分"。

---

### 6.7 误差为什么是「异方差」的

功率预测的误差大小**取决于风速**：

| 风速区间 | 误差 | 原因 |
|---|---|---|
| < 3 m/s | 小 | 出力恒为 0，蒙对 |
| 3~12 m/s | **大** | 爬坡段，v³ 敏感，最活跃 |
| > 12 m/s | 小 | 出力饱和在额定值，也容易蒙对 |

**所以只报总体 RMSE 是不够的**，要**按风速分档报误差**，
才能看出模型弱在哪。→ 见 `evaluate.py::error_by_wind_bin`

> 📌 这个细节很多候选人都不知道，**说出来很加分**。

---

## 七、怎么运行

### 环境准备

```bash
# 建议 Python 3.9+
pip install -r requirements.txt

# 下载数据（约 6.6 MB，10 个风场）
python data/download.py
```

### 一键运行

```bash
python run.py
```

会依次输出：数据统计 → 特征工程 → 切分点 → 各模型指标 → 对比表 → 分档误差

**可选参数**：

```bash
python run.py --zones 1 2 3       # 只用前 3 个风场（跑得快）
python run.py --test-ratio 0.3    # 测试集占 30%
```

### 输出

- `results/model_comparison.csv` —— 模型对比表（贴 README 用）
- `results/*.png` —— 四张图

---

## 八、结果怎么读

> ✅ **这一节写的是你实际跑出来的结果**（10 个风场，前 70% 训练）。

### 实验 A【纯 NWP 预报】

| 模型 | RMSE | 准确率% | 合格率% |
|---|---|---|---|
| **物理基线·功率曲线** | **0.1949** | **80.51** | **82.56** |
| LightGBM | 0.1991 | 80.09 | 80.71 |
| Ridge 回归 | 0.1998 | 80.02 | 81.04 |
| 对照组·风速线性回归 | 0.2010 | 79.90 | 80.22 |
| 物理基线·纯立方律 | 0.2387 | 76.13 | 74.58 |

### 实验 B【NWP + 近期实测】

| 模型 | RMSE | 准确率% |
|---|---|---|
| Ridge 回归 | 0.0938 | 90.62 |
| LightGBM | 0.0942 | 90.58 |
| 基线·持续性 | 0.1003 | 89.97 |

---

### 🔥 你的三个结论（这就是你面试要讲的东西）

**① 纯 NWP 场景下，物理基线打败了机器学习**

功率曲线 80.51% > LightGBM 80.09%，ML 提升 **−0.42** 个百分点。

**怎么解释**（这段要背下来）：
> 输入只有 4 个 NWP 变量，而功率曲线恰好是这 4 个变量到出力的**物理最优映射**。
> 树模型在这点信息量上榨不出更多东西，反而被噪声带偏。
> **说明在特征信息有限时，正确的物理先验比模型复杂度更值钱。**

⚠️ 不要把这个当成失败。**这是一个诚实的、反直觉的结果** ——
面试官见过太多"我的模型比基线好 30%"的假结论，
你却拿出一个"我的模型没打过物理基线，原因是……"——
**后者可信得多。**

**② 持续性基线很强，必须拿它当门槛**

实验 B 里持续性 89.97%，ML 只比它高 0.6 个百分点。
**如果你的 ML 连持续性都打不过，说明模型白做了。**
很多论文号称提升 30% 却没跟持续性比 —— 一比就露馅。

**③ 误差集中在爬坡段，符合 v³ 物理预期**

分档误差（实验 A）：

| 风速区间 | RMSE | 平均出力 |
|---|---|---|
| (0, 3] | 0.0637 | 0.02 |
| (3, 6] | 0.1431 | 0.14 |
| **(6, 9]** | **0.2274** | 0.46 |
| (9, 12] | 0.2130 | 0.74 |
| (12, 25] | 0.2288 | 0.82 |

两端（无风、饱和）误差小，中间爬坡段误差最大。
**这是物理决定的，不是模型不行** —— 能说出这句话，说明你真懂。

---

### 你要检查的三件事

1. **模型排序对不对** —— 实验 A 里功率曲线第一，说明物理基线构造正确
2. **分档误差有没有呈现"中间高、两端低"** —— 有，说明符合预期
3. **特征重要性里 shear 排第几** —— 见下，**排第 2**

---

### 🏆 特征重要性：你的物理特征被模型认证了

实验 A（纯 NWP）跑出来的排序：

```
doy_sin   4600   年周期（季节变化）
shear     4090   ← 风切变，排第 2
doy_cos   3429
ws100     3365   ← 正确合成的风速
u100      2853   ← 原始分量
u10       2812
v10       2800
ws10      2785
v100      2680
wd10      2085
```

**两个你要记住的结论**：

**① 风切变排第 2**
模型不是随便给个位置 —— 说明 `α` 确实携带了它需要的信息
（大气边界层稳定度的昼夜变化），不是冗余变量。
**这是你「懂物理」的硬证据**，比说一百句"我学过大气物理"管用。

**② `ws100` 排在 `u100` / `v100` 前面**
模型本来也能从 U/V 分量里自己学出风速（它是全连接/树结构），
但**显式构造物理量仍然更有效**。
这印证了特征工程的价值：**把物理先验直接告诉模型，比让它自己猜快得多。**

> 📌 **面试话术**：「我做了特征重要性分析，风切变指数排到第二位，
> 说明它确实捕捉到了边界层稳定度的信息。
> 另外我合成的风速排在原始 U/V 分量前面，
> 说明显式构造物理量比让模型自己学更有效。」

### 项目当前的诚实短板（写进 README 的"后续计划"）

- 不同 Task 只用了 **Task 1**，样本只有 6577 小时 × 10 风场
- 没做**概率预测**（只给单点，没给区间）
- 没跟**深度学习**比过
- 空间相关性没利用（10 个风场之间是有关系的）

**主动说出短板，比装完美强。** 面试官问"你觉得哪里还能改进"，
你就答这些。

---

## 九、面试时怎么讲

**30 秒版本**（电梯陈述）：

> 我用 GEFCom2014 的风电数据做了一个功率预测项目。
> 核心思路是先用 NWP 的 U/V 分量还原出真实风速和风切变，
> 再按风电行业的功率曲线法建物理基线，最后上 LightGBM。
> 我特别注意了两点：一是按时间切分避免数据泄漏，
> 二是跟持续性基线做对比，确认模型真的有效。

**被追问时的弹药**：

| 面试官问 | 你答 |
|---|---|
| 为什么不用深度学习？ | 表格数据上树模型通常更强；而且 LightGBM 训练快、可解释，能出特征重要性。上 LSTM 我得先证明它打得过 LightGBM |
| 你加的最有价值的特征是什么？ | 风切变指数。因为风机在 100m，而边界层廓线随稳定度变化 |
| 你最大的坑是什么？ | 时间戳格式不规范，还有 U/V 分量不能当风速用 |
| 结果提升多少？ | （报你自己的数）相对物理基线提升 X 个百分点 |
| 怎么保证不是过拟合？ | 时间序列切分 + 算持续性基线 + 分档看误差 |

---

## 十、接下来可以做什么

按**性价比**排序，做完前两个就很够了：

**⭐ 1. 概率预测**
不只给一个数，给**预测区间**（比如 80% 置信区间）。
GEFCom2014 的 Task 15 就是概率预测任务，官方有评分标准（Pinball Loss）。
**这是行业趋势**，电网更关心不确定性。

**⭐ 2. 扩展到 15 个 Task**
现在只用了 Task 1。把 15 个 Task 都用上，样本量翻十倍，
而且能做**滚动预测**实验。

**3. 加更多特征**
- 空间特征：邻近风场的数据（10 个风场之间有空间相关）
- 多高度层：用 10m 和 100m 外推其他高度
- 天气分型：按环流形势聚类

**4. 换深度学习**
Temporal Fusion Transformer (TFT)、Informer、PatchTST。
**注意**：一定要先证明它打得过 LightGBM，否则别放进去。

**5. 做成一个小服务**
Flask/FastAPI 包一层，输入风速返回预测出力。
**加分点**：面试官看到这个会认为你有工程能力。

---

## 附：你该记住的三句话

1. **出力与风速是三次方关系** —— 所以气象精度决定风电预测天花板
2. **必须按时间切分** —— 随机切分是数据泄漏，指标虚高
3. **物理特征是你相对纯 CS 候选人的护城河** —— 风切变、功率曲线、边界层

---

*有看不懂的地方，标出来。别装懂 —— 面试官一问就穿。*
